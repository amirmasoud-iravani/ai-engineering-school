# Data preprocessing projects

The first project builds a leakage-safe preprocessing system for a small Persian learner corpus. It combines data contracts, quality audits, group splitting, aligned text views, mixed-column pipelines, and validation checks.

Begin with `learner_corpus_preprocessor/`.
