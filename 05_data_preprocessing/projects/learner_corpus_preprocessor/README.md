# Learner-corpus preprocessing project

Open `01_learner_corpus_preprocessor.ipynb` and run it from top to bottom.

The final output is a set of model-ready train, validation, and test matrices plus a documented feature contract. No model is fitted; Stage 6 will reuse this structure for classification and evaluation.
